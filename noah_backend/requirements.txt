# Runtime
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-dotenv>=1.0.1
httpx>=0.27.0
groq>=0.5.0
pypdf>=4.1.0

# NLP pipeline.
# Pinned: route.joblib is a pickled scikit-learn pipeline, and unpickling it
# under a different minor version raises InconsistentVersionWarning and can
# silently change predictions. Retrain (python -m app.nlp.train) if you bump these.
scikit-learn==1.7.2
joblib==1.5.2
numpy>=1.26.0

# Corpus build + training only (scripts/build_dataset.py, app/nlp/train.py).
# Not imported by the request path.
pandas>=2.2.0

# Removed 2026-09: sentence-transformers, faiss-cpu (pulled in torch, ~900 MB,
# never imported - retrieval is TF-IDF), firebase-admin, orjson, loguru and
# ollama. None appear in an import statement anywhere in the codebase.
